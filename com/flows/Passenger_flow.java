package com.flows;

import com.pages.Passenger;

public class Passenger_flow {
Passenger pas=new Passenger();

public void Passengrs(String name,String lastname,String mealtype) {
	pas.firstname(lastname);
	pas.lastname(lastname);
	pas.mealoption(mealtype);
}
}
